# MIG-PLAN-001 Repository Verification

**Date:** 2026-07-28
**Task:** `MIG-PLAN-001`
**Status:** VALIDATED WITH SECURITY FOLLOW-UP
**Scope:** Read-only verification before Track B migration drafting

## Verified Baseline

- Historical migrations `001-034` are present and unchanged.
- Current local migration history matches the repository through `20260728152602_a3_returns_assignment_boundary.sql`.
- Post-034 migrations are Track A/shared hardening and A3 assignment boundaries; no Track B production migration exists.
- The next migration must use the next actual repository timestamp generated by the Supabase CLI. The roadmap label `035+` is a logical Track B range, not a filename to hardcode.

## Verified Core Tables

The local schema contains the canonical tables required for Track B references:

```text
organizations
profiles
organization_memberships
customers
products
product_variants
orders
roles
permissions
role_permissions
```

Track B must reference these tables and must not create duplicate Customer, Product, Order, Organization, or membership masters.

## Verified Schema Conventions

- Tenant-owned tables use `organization_id uuid not null`.
- Tenant-scoped identity FKs use composite `(organization_id, id)` or `(organization_id, profile_id)` references where cross-tenant integrity is required.
- Status fields use `varchar`/text with named `CHECK` constraints. No public PostgreSQL enums are currently used by the verified baseline.
- IDs use `uuid`; timestamps use `timestamptz`; monetary values use `numeric`.
- Unique tenant keys use `(organization_id, business_key)` indexes.
- RLS is enabled on the verified Core tables.

## Verified RLS and Permission Conventions

- Existing policies combine tenant membership or permission predicates; `authenticated` permission policies use `has_org_permission(organization_id, permission_code)`.
- Existing helper names are `is_org_member`, `current_profile_id`, and `has_org_permission`.
- Permission keys are seeded in `public.permissions.code` and linked through `roles`, `membership_roles`, and `role_permissions`.
- Track B permission additions must use the existing permission seed pattern and least privilege.

## Security Follow-up

Existing helper functions are `SECURITY DEFINER` with `search_path=public`. This is a legacy baseline convention and is not a safe template for new privileged functions. New Track B functions must:

- use `SECURITY DEFINER` only when required;
- set a secure empty search path;
- schema-qualify every referenced object;
- revoke `PUBLIC`/`anon` execute and grant only the intended role;
- include authenticated actor, tenant, permission, idempotency, and audit guards where applicable.

This follow-up does not alter historical migrations or modify the existing helpers in MIG-PLAN-001.

## Result

`MIG-PLAN-001` is `VALIDATED`. Track B migration drafting may be planned one file at a time, but no SQL is generated by this verification task.

## Next Gate

Prepare the first Track B migration contract for Owner review, then generate the migration with the Supabase CLI using the current repository state. Begin with Content Core only; keep RLS, permissions, events, messaging, and usage extensions in their approved dependency order.

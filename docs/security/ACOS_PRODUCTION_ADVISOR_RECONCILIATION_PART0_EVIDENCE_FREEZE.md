# ACOS Production Advisor Reconciliation
# Part 0 - Evidence Freeze and Classification

**Date:** 2026-07-30
**Status:** VALIDATED / EVIDENCE FROZEN
**Target:** `ACOS Production` (`pirewyrhddrhmtiwmlaw`)
**Scope:** Read-only advisor evidence and provisional classification

---

## 1. Guardrails

Part 0 did not:

```text
create or alter database objects
change grants
edit historical migrations
push a new migration
connect Vercel environment variables
enable production signup
```

The production migration history remained synchronized at 83/83 before this
classification.

---

## 2. Evidence Command

```text
supabase db advisors --linked --type all --level warn --fail-on none
```

Observed result:

```text
ERROR: 0
WARN: 44
```

| Advisor | Count | Part 0 classification | Next part |
|---|---:|---|---|
| Anonymous SECURITY DEFINER executable | 1 | REMEDIATE | Part 1 |
| Authenticated SECURITY DEFINER executable | 37 | 1 REMEDIATE + 36 CONTRACT REVIEW REQUIRED | Parts 1-2 |
| Mutable function search path | 2 | REMEDIATE | Part 1 |
| Extension in `public` | 2 | DEPENDENCY REVIEW REQUIRED | Part 3 |
| RLS initplan performance | 2 | REMEDIATE WITHOUT SEMANTIC CHANGE | Part 3 |

---

## 3. Part 1 Remediation Set

### `public.rls_auto_enable()`

Findings:

```text
anon_security_definer_function_executable
authenticated_security_definer_function_executable
```

Classification:

```text
REMEDIATE
```

Reason:

```text
This function was generated by the Supabase automatic-RLS project setting.
It is not an approved ACOS browser RPC and has no repository API contract.
Anonymous or ordinary authenticated callers must not execute schema-level
automation.
```

Required Part 1 decision:

```text
Revoke EXECUTE from PUBLIC, anon and authenticated while preserving the
database event-trigger execution path.
```

### Mutable search path

| Function | Source | Classification |
|---|---|---|
| `public.set_updated_at()` | `supabase/migrations/001_extensions_helpers.sql` | REMEDIATE |
| `public.prevent_update_delete()` | `supabase/migrations/001_extensions_helpers.sql` | REMEDIATE |

Required Part 1 decision:

```text
Set an immutable, minimal search_path through a forward migration and prove
existing updated-at and append-only triggers retain their behavior.
```

---

## 4. Part 2 Contract Review Set

The following 36 findings are not automatically vulnerabilities. Repository
migrations explicitly revoke `PUBLIC`/`anon` access and grant
`authenticated` access because these functions are guarded RPC boundaries.
Part 2 must verify the complete in-function contract before accepting or
changing each grant.

Classification for every item below:

```text
CONTRACT REVIEW REQUIRED
```

### Auth and permission helpers (3)

```text
current_profile_id
is_org_member
has_org_permission
```

Primary source:

```text
supabase/migrations/032_rls_helpers.sql
supabase/migrations/20260726185117_035_security_rls_hardening.sql
```

### Inventory and product cost (6)

```text
api_reserve_inventory
api_release_inventory_reservation
api_convert_reservation_to_allocation
api_post_inventory_movement
api_get_product_variant_cost
api_update_product_variant_cost
```

Primary sources:

```text
supabase/migrations/20260726194356_inventory_transaction_wrappers.sql
supabase/migrations/20260726195240_product_cost_wrappers.sql
```

### Operations and assignments (10)

```text
api_process_refund
api_override_qc_session
api_create_shipment_label
api_complete_qc_session
api_mark_shipment_ready_for_handoff
api_record_carrier_tracking_event
api_assign_fulfillment
api_assign_qc_session
api_assign_shipment
api_assign_return
```

Primary sources:

```text
supabase/migrations/20260726201809_guarded_operations_wrappers.sql
supabase/migrations/20260726202729_shipping_workflow_wrappers.sql
supabase/migrations/20260726203930_carrier_webhook_boundary.sql
supabase/migrations/20260728135454_a3_fulfillment_assignment_boundary.sql
supabase/migrations/20260728143613_a3_warehouse_qc_assignment_boundary.sql
supabase/migrations/20260728150119_a3_shipping_assignment_boundary.sql
supabase/migrations/20260728152602_a3_returns_assignment_boundary.sql
```

### Member administration (8)

```text
api_request_member_invitation
api_prepare_member_invitation_email_send
api_record_member_invitation_email_event
api_accept_member_invitation
api_assign_member_role
api_remove_member_role
api_replace_member_role
api_deactivate_member
```

Primary sources:

```text
supabase/migrations/20260727120000_member_invite_request_rpc.sql
supabase/migrations/20260727172407_member_invite_auth_admin_email_boundary.sql
supabase/migrations/20260727181500_member_invite_acceptance_activation_boundary.sql
supabase/migrations/20260727190000_member_role_assignment_boundary.sql
supabase/migrations/20260727193000_member_role_removal_boundary.sql
supabase/migrations/20260728120110_a3_role_replacement_boundary.sql
supabase/migrations/20260728133224_a3_member_deactivation_boundary.sql
```

### Customer portal and ownership (9)

```text
api_get_customer_portal_snapshot
api_create_customer_portal_address
api_update_customer_portal_address
api_archive_customer_portal_address
api_update_customer_portal_consent
api_request_customer_contact_change
api_get_customer_portal_notifications
api_request_customer_profile_link
api_revoke_customer_profile_link
```

Primary sources:

```text
supabase/migrations/20260728193910_customer_profile_link_guarded_actions.sql
supabase/migrations/20260728195007_customer_portal_read_snapshot_boundary.sql
supabase/migrations/20260728201851_customer_portal_address_guarded_actions.sql
supabase/migrations/20260728203316_customer_portal_consent_guarded_action.sql
supabase/migrations/20260728204359_customer_portal_verified_contact_and_notification_mapping.sql
```

Part 2 acceptance criteria for each function:

```text
authenticated caller identity is checked
tenant organization is checked
active membership or active customer-profile ownership is checked
required permission is checked where applicable
resource tenant/ownership is checked
idempotency and audit requirements are enforced where applicable
PUBLIC and anon remain denied
server-only functions remain service_role-only
negative tests cover cross-tenant and unauthorized calls
```

---

## 5. Part 3 Review Set

### Extensions

```text
pg_trgm
unaccent
```

Classification:

```text
DEPENDENCY REVIEW REQUIRED
```

No extension will be moved merely to silence an advisor. Part 3 must inspect
dependent indexes, functions and replay behavior before proposing a forward
migration.

### RLS performance

```text
profiles_self_select
profiles_self_update
```

Classification:

```text
REMEDIATE WITHOUT SEMANTIC CHANGE
```

Part 3 may replace per-row auth evaluation with an initplan-safe
`(select auth.uid())` form only after proving the policy predicates and
self-access behavior remain identical.

---

## 6. Frozen Part Order

```text
Part 1: Critical exposure and search_path hardening
Part 2: Guarded RPC contract reconciliation
Part 3: Extension dependency and RLS performance review
Part 4: Full replay, production push, advisors and status closure
```

Vercel-to-Supabase credentials remain blocked until Part 4 closes all
unexpected security findings or records an approved, contract-backed
disposition.
